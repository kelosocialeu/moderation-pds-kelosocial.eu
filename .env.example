# Never commit real values.
SESSION_SECRET=replace-with-at-least-32-random-characters
ADMIN_PASSWORD_HASH=salt:64-byte-scrypt-hash-in-hex

# Server-to-server moderation service connection.
MODERATION_API_URL=https://mod.kelosocial.eu
MODERATION_API_TOKEN=replace-with-server-only-token
